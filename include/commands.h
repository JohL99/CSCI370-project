/**
 * @file commands.h
 * @brief Defines the commands used in the application.
 */

//general commands
#define quit "quit" /**< Command to quit the application. */
#define back "back" /**< Command to go back to the previous screen. */
#define help "help" /**< Command to display the help menu. */

// welcome screen commands
#define viewAppointments "view" /**< Command to view appointments. */
#define bookAppointment "book" /**< Command to book an appointment. */

// view appointments screen commands
#define cancelAppointmentCmd "cancel" /**< Command to cancel an appointment. */
#define confirm "confirm" /**< Command to confirm an appointment. */

// --------------------------------------------------------------------------------
// stretch goals
// #define downloadReport "download" /**< Command to download a report. */
// --------------------------------------------------------------------------------

